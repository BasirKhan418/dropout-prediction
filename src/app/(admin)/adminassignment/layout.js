import React from 'react'
export const metadata = {
    title: "Add Assignment | DropGuard LMS",
    description: "Create and manage assignments for your courses on DropGuard LMS. Add new assignments, set deadlines, and provide instructions to students.",
    keywords: "add assignment, LMS assignments, create assignment, manage assignments, DropGuard LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
