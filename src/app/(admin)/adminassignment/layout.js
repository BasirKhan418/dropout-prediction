import React from 'react'
export const metadata = {
    title: "Add Assignment | DevsIndia LMS",
    description: "Create and manage assignments for your courses on DevsIndia LMS. Add new assignments, set deadlines, and provide instructions to students.",
    keywords: "add assignment, LMS assignments, create assignment, manage assignments, DevsIndia LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
