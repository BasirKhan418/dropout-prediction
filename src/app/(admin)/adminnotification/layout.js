import React from 'react'
export const metadata = {
    title: "Send Notification | DropGuard LMS",
    description: "Send notifications to users regarding important updates, deadlines, and announcements through DropGuard LMS.",
    keywords: "send notification, LMS notifications, user notifications, DropGuard LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
