import React from 'react'
export const metadata = {
    title: "Send Notification | DevsIndia LMS",
    description: "Send notifications to users regarding important updates, deadlines, and announcements through DevsIndia LMS.",
    keywords: "send notification, LMS notifications, user notifications, DevsIndia LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
