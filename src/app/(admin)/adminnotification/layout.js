import React from 'react'
export const metadata = {
    title: "Send Notification | RadSab LMS",
    description: "Send notifications to users regarding important updates, deadlines, and announcements through RadSab LMS.",
    keywords: "send notification, LMS notifications, user notifications, RadSab LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
