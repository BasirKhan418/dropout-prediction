import React from 'react'
export const metadata = {
    title: "Add Project | DevsIndia LMS",
    description: "Add and manage projects for your courses. Provide details, deadlines, and instructions for student projects on DevsIndia LMS.",
    keywords: "add project, LMS projects, create project, manage projects, DevsIndia LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
