import React from 'react'
export const metadata = {
    title: "Add Project | DropGuard LMS",
    description: "Add and manage projects for your courses. Provide details, deadlines, and instructions for student projects on DropGuard LMS.",
    keywords: "add project, LMS projects, create project, manage projects, DropGuard LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
