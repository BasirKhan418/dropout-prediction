import React from 'react'
export const metadata = {
    title: "Admin Profile | RadSab LMS",
    description: "View and manage your admin profile. Update your information, settings, and preferences on RadSab LMS.",
    keywords: "admin profile, LMS admin profile, manage profile, RadSab LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
