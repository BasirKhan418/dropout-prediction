import React from 'react'
export const metadata = {
    title: "Admin Profile | DropGuard LMS",
    description: "View and manage your admin profile. Update your information, settings, and preferences on DropGuard LMS.",
    keywords: "admin profile, LMS admin profile, manage profile, DropGuard LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
