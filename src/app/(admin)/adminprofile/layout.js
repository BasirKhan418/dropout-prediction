import React from 'react'
export const metadata = {
    title: "Admin Profile | DevsIndia LMS",
    description: "View and manage your admin profile. Update your information, settings, and preferences on DevsIndia LMS.",
    keywords: "admin profile, LMS admin profile, manage profile, DevsIndia LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
