import React from 'react'
export const metadata = {
    title: "Add Course Content | DropGuard LMS",
    description: "Upload and manage course content including lectures, videos, and resources. Enhance your course offerings on DropGuard LMS.",
    keywords: "add course content, LMS course content, upload content, manage course resources, DropGuard LMS"
  };
  
const layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
